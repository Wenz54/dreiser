events {
    worker_connections 4096;
}

http {
    resolver 127.0.0.11 valid=10s;
    resolver_timeout 5s;
    
    # Binance WebSocket SSL termination
    server {
        listen 8080;
        server_name localhost;
        
        location / {
            set $upstream_binance https://stream.binance.com;
            proxy_pass $upstream_binance$request_uri;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host stream.binance.com;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_ssl_server_name on;
            proxy_read_timeout 86400;
        }
    }
    
    # MEXC WebSocket SSL termination
    server {
        listen 8081;
        server_name localhost;
        
        location / {
            set $upstream_mexc https://wbs.mexc.com;
            proxy_pass $upstream_mexc$request_uri;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host wbs.mexc.com;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_ssl_server_name on;
            proxy_read_timeout 86400;
        }
    }
    
    # Bybit WebSocket SSL termination
    server {
        listen 8082;
        server_name localhost;
        
        location / {
            set $upstream_bybit https://stream.bybit.com;
            proxy_pass $upstream_bybit$request_uri;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host stream.bybit.com;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_ssl_server_name on;
            proxy_read_timeout 86400;
        }
    }
    
    # OKX WebSocket SSL termination
    server {
        listen 8083;
        server_name localhost;
        
        location / {
            set $upstream_okx https://ws.okx.com:8443;
            proxy_pass $upstream_okx$request_uri;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host ws.okx.com;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_ssl_server_name on;
            proxy_read_timeout 86400;
        }
    }
    
    # Gate.io WebSocket SSL termination
    server {
        listen 8084;
        server_name localhost;
        
        location / {
            proxy_pass https://api.gateio.ws;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host api.gateio.ws;
            proxy_ssl_server_name on;
            proxy_read_timeout 86400;
        }
    }
    
    # KuCoin WebSocket SSL termination
    server {
        listen 8085;
        server_name localhost;
        
        location / {
            proxy_pass https://ws-api-spot.kucoin.com;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host ws-api-spot.kucoin.com;
            proxy_ssl_server_name on;
            proxy_read_timeout 86400;
        }
    }
    
    # Huobi WebSocket SSL termination
    server {
        listen 8086;
        server_name localhost;
        
        location / {
            proxy_pass https://api.huobi.pro;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host api.huobi.pro;
            proxy_ssl_server_name on;
            proxy_read_timeout 86400;
        }
    }
    
    # Bitget WebSocket SSL termination
    server {
        listen 8087;
        server_name localhost;
        
        location / {
            set $upstream_bitget https://ws.bitget.com;
            proxy_pass $upstream_bitget$request_uri;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host ws.bitget.com;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_ssl_server_name on;
            proxy_read_timeout 86400;
        }
    }
}


